
var players_list = List.filled(4,"NULL");


  var all_rounds_scores = List.generate(1, (i) => List.filled(4,0), growable: true);
  var total_scores = List.filled(4,0);
  var curr_player_eat = List.filled(4,0);